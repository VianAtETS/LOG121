{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Delete old Git branches",
            "type": "shell",
            "command": "git fetch --prune && git fetch -p && git branch -vv | awk '/: gone]/{print $1}' | xargs -r git branch -D",
            "problemMatcher": [],
            "presentation": {
                "showReuseMessage": false
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
